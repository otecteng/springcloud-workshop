spring:
  application:
    name: product
  cloud:
    consul:
      host: localhost
      port: 8500
      enabled: true
      ribbon:
        enabled: true
      discovery:
        enabled: true
        register: true
        preferIpAddress: true
        heartbeat:
          enabled: true
          ttlValue: 10
      config:
        enabled: false